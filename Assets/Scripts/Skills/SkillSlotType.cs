/// <summary>
/// Identifies which typed slot an <see cref="AllySkill"/> belongs in.
/// Each ally has exactly one slot of each type.
/// </summary>
public enum SkillSlotType
{
    BasicAttack,
    AdvancedAttack,
    Defensive,
    Support
}
